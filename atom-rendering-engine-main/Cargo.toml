# Atom Rendering Engine — a self-renderer from pure math primitives.
# Two members only, per the Composition doctrine: one kit + one orchestrator.
[workspace]
resolver = "2"
members = ["pmre-kit", "pmre-orchestrator"]
